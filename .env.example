# Optional: n8n or webhook endpoint for cost reports
# WEBHOOK_ENDPOINT=https://your-n8n-instance.com/webhook/aws-costs

# Optional: Email address for SNS notifications
# NOTIFICATION_EMAIL=your-email@example.com

# AWS Configuration (automatically set by CDK CLI)
# CDK_DEFAULT_ACCOUNT=123456789012
# CDK_DEFAULT_REGION=us-east-1

# API Key for Cost Reporter API
# Find it in AWS Console → API Gateway → API Keys → cost-reporter-api-key-{unique-id}
# Or use CLI: aws apigateway get-api-key --api-key <KEY_ID> --include-value --query 'value' --output text
# AWS_COST_REPORTER_API_KEY=your_api_key_here
